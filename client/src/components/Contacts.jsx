import React from "react";

import '../App.css';
import './Contact.css';

const Contacts = () => {
    return(
        <div id='contacts' className="contacts">
            <h1>Contacts</h1>
        </div>
    );
}

export default Contacts;